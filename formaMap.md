# [Project Spec & Status] Forma-Korea-Map: 한국 공공 지적도 Forma 자동 임포터 및 대지 분석 시스템

## 1. 프로젝트 개요 (Project Overview)
* **목적**: Autodesk Forma 기본 환경에서 부정확하거나 누락된 한국 지역의 연속지적도 데이터를 VWorld 오픈 API를 통해 실시간으로 수집하고, WGS84 좌표 변환을 거쳐 Forma 캔버스에 `site_limit` 및 `constraint` 요소로 자동 투영하는 풀스택 익스텐션 시스템 구축.
* **진행 목표**: 단순 데이터 임포트를 넘어, 한국 건축법규(정북방향 일조권 사선제한 등)에 맞춘 **대지 및 주변 현황 자동 분석 기능**을 제공하는 형태로 발전 중.

---

## 2. 현재까지의 진행 상황 (진척도 요약)

### [완료] 당초 계획된 코어 기능 (Data Pipeline)
1. **Backend 환경 및 VWorld API 연동**
   - FastAPI + GeoPandas 기반 마이크로서비스 구축 완료.
   - VWorld Open API(LT_C_DGMT_CADASTRE)를 호출하여 BBox 기반 연속지적도 데이터 수집 완료.
   - EPSG:5179 -> EPSG:4326(WGS84) 좌표 변환 및 GeoJSON 직렬화 파이프라인 정상 가동.
2. **Frontend 익스텐션 기본 셋업**
   - React + Vite + TypeScript 기반 패널 UI 구축.
   - WGS84 위경도를 Forma 프로젝트 기준점(refPoint)을 활용해 Local Cartesian 좌표(X, Y 미터)로 변환하는 알고리즘(`wgs84ToLocalMeters`) 완벽 작동.
   - Forma 캔버스에 `site_limit` 요소로 VWorld 지적도 데이터 투영 완료.

### [완료] 추가 확장 기능 (대지 분석 리포트 - UI & 기하학 로직)
당초 단순 지적선 임포트에서 나아가, 한국 실무에 필수적인 **대지 분석 엔진(SiteInfoPanel.tsx)**을 고도화하여 구축 완료함.
1. **기본 대지 정보 분석**
   - 타겟 대지 면적 자동 산출 로직 적용 (shoelace algorithm).
   - Forma Terrain API(`Forma.terrain.getElevationAt`)를 호출하여 대지 꼭짓점별 해발고도(EL) 자동 추출 및 최대 고저차 계산.
   - 토지이용계획(용도지역) 및 인접 도로(지목 '도') 자동 탐지.
2. **정북방향 일조권 사선제한 분석 (핵심 달성)**
   - 북측에 위치한 인접 대지들을 자동으로 판별 및 추출.
   - **(가장 큰 난제 해결)** 북측 경계선 판별 알고리즘 고도화: 단순 방위각이 아닌, 실무적 관점에서의 수직 벽면(동/서측)을 걸러내는 기울기 필터링(45도 초과 경사선 제외) 완벽 구현.
   - 건축대지의 서측/동측 선 및 인접지 간 공유 경계선을 가상의 직선으로 연장(Raycasting)하여, 북측 대지 경계선과 만나는 투영점(A', B', C')을 정확히 찾아내는 수학적 로직 완성 (사용자의 수기 도면과 100% 일치).
   - 각 구간별 시작점/끝점의 가중 평균 레벨 및 전체 묶음의 **"최종 북측 일조사선 평균레벨"** 자동 계산 및 SVG 도면 시각화 완료.

---

## 3. 남은 과제 (Remaining Tasks)

당초 계획 및 확장 논의된 사항 중 미구현 상태인 기능들입니다.
1. **건축물 매스 디자인 (Building Design)**
   - 5번째 탭으로 예정됨. 
   - 분석된 대지 정보와 일조권 사선제한 볼륨을 바탕으로 실제 건축물 매스를 모델링하거나 배치해 볼 수 있는 기능 연동 필요.
2. **최종 법규 검토 (Final Compliance Review)**
   - 6번째 탭으로 예정됨.
   - 용적률(FAR), 건폐율(Coverage) 등 법정 허용치 대비 계획된 건축물의 수치를 비교 및 검증하는 UI 및 산식 구현.
3. **일조권 3D Constraint 생성 (선택 사항)**
   - 현재 정보 패널(2D SVG)에 시각화된 북측 일조권 평균 레벨 및 사선 제한선을 Forma 3D 캔버스에 투명한 Constraint Volume(규제 볼륨) 형태로 실제 Generate 하는 기능.

---

## 4. 주요 확인된 사항 및 기술적 어려움 (Key Findings & Challenges)

1. **지적도의 위상학적 불일치 (Topological Imperfections)**
   - VWorld 데이터 특성상 인접한 필지라도 폴리곤 꼭짓점 좌표가 미세하게 일치하지 않음.
   - **해결 방안:** 점 간 거리를 계산해 허용 오차(tolerance) 내에 있으면 같은 점으로 병합(deduplication)하거나, 선분과의 최단 거리(`distToSegmentSquared`)가 1.0m 이내일 경우 공유 경계로 인식하는 방식으로 극복함.
2. **실무적 법규 해석의 알고리즘화 (The 'North' Boundary Problem)**
   - 기하학적으로는 북쪽을 향하더라도, 꺾인 각도가 가파르면(예: 45도 이상) 실무에서는 '북측 선'이 아닌 '동/서측 선'으로 취급함.
   - 초기엔 북측으로 쏜 Ray가 겹치는 선분을 모두 북측 선으로 간주했으나, 건축 실무와 맞지 않아 오류 발생.
   - **해결 방안:** 두 점 사이의 dx, dy 기울기를 분석하여 `Math.abs(dx) < Math.abs(dy) * 1.1` (45도 이상 수직에 가까운 벽면)인 경우 북측 경계선 계산에서 엄격하게 탈락시키는 룰을 추가하여 완벽히 해결.
3. **가상 투영점(Prime Points) 계산의 기하학적 복잡도**
   - 단순한 점 평행이동이 아니라, 타겟 대지의 서측/동측 경계선의 '각도'를 그대로 허공으로 쏘아올려(Raycasting) 인접 대지의 북측 선과 교차하는 점을 찾아야 했음.
   - **해결 방안:** 벡터 연산과 `rayIntersectPolygon` 알고리즘을 직접 구현하여, 건축사가 CAD에서 선을 연장(Extend)하여 만나는 교점을 찾는 행위를 코드로 완벽히 재현함.
4. **Forma API 의존성 캐싱 (Hot Reload 이슈)**
   - 코드를 저장할 때마다 Forma Extension이 재구동되면서 API 호출 제한이나 상태 초기화 문제가 발생함.
   - **해결 방안:** `localStorage`를 활용하여 대상지와 컨텍스트 폴리곤 데이터를 Element ID 기준으로 캐싱 처리하여 네트워크 낭비와 렌더링 지연을 최소화함.